{
  "name": "Basic Node.js",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "node",
  "postCreateCommand": "npm install -g @angular/cli",
  "customizations" : {
    "jetbrains" : {
      "backend" : "WebStorm"
    }
  }
}
